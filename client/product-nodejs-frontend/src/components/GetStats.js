import React, { useState, useEffect } from 'react';

export default function GetStats() {

    return (
        <div>
            {/* <button onClick={() => setUrl('stats/hourly')}>
                Click Me!
            </button>
            <button onClick={() => setUrl('stats/daily')}>
                Click Me!
            </button> */}
        </div>
    );
};
