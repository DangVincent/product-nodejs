import React, { useState, useEffect } from 'react';

export default function GetPOI() {

    return (
        <div>
            {/* <button onClick={() => setUrl('poi')}>
                Click Me!
            </button> */}
        </div>
    );
};
